schema: 1
slug: gitlab-mcp
name: 'GitLab MCP Server'
version: 1.0.0
tier: community
provenance: generated
maintainers: [aliosmanmho]
summary: "GitLab's built-in MCP server (beta): projects, work items, merge requests and pipelines on your own GitLab instance or GitLab.com."
kind: mcp
upstream: https://gitlab.com/gitlab-org/gitlab
upstream_version: 'gitlab-18.6'
verified_on: 2026-09-24
install:
  claude-code: 'claude mcp add -s user --transport http GitLab "https://<your-gitlab-host>/api/v4/mcp"'
  codex: 'codex mcp add GitLab --url "https://<your-gitlab-host>/api/v4/mcp"'
  gemini-cli: 'gemini mcp add -t http GitLab "https://<your-gitlab-host>/api/v4/mcp"'
permissions_summary: 'Signs in as you through OAuth with the mcp scope and acts with your GitLab permissions: reads projects, work items, merge requests and pipelines, and can commit, open and merge merge requests, and run or delete pipelines.'
fits: [software, devops, product]
agents: [claude-code]
deprecated: false
supersedes: null
