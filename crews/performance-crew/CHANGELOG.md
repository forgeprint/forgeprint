# Changelog

## 1.0.0 — 2026-09-25

Phase 9 of the 2026-09-24 expansion plan.

- `observability-engineer`, `performance-engineer` (the checker, measuring every change against its baseline and spread), `postgres-database-administrator` and `frontend-engineer`. One change in flight at a time.
- Scoped to PostgreSQL-backed web applications, because the DBA member is pinned to PostgreSQL.
- Three integrations: `chrome-devtools-mcp`, `sentry-mcp`, `postgres-mcp`.
- `not_for` names the small, sequential and same-file work where more agents measurably do worse, and the README cites MAST and Kim et al. for why the crew stays small.
- `tier: community`, `provenance: generated` (ADR 0011).
