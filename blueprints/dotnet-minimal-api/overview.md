# ASP.NET Core Minimal API

## What it fits

Services that speak JSON over HTTP, authenticate with a bearer token, and ship
as a container image.

## What it is NOT for

Server-rendered web applications, and anything that needs tenant isolation.

## Trade-offs made on your behalf

Minimal APIs rather than controllers, and EF Core rather than a micro-ORM.
